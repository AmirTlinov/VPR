# Build Targets

- `cargo build --release` builds:
  - `target/release/masque-core`
  - `target/release/doh-gateway`
  - `target/release/health-harness`
- `scripts/build_release.sh` wraps the same command for GUI deploy.
