# Contributing to VPR

Спасибо за интерес к проекту VPR! Этот документ описывает процесс внесения вклада в проект.

## Содержание

1. [Код поведения](#код-поведения)
2. [Как внести вклад](#как-внести-вклад)
3. [Стандарты разработки](#стандарты-разработки)
4. [Процесс разработки](#процесс-разработки)
5. [Тестирование](#тестирование)
6. [Документация](#документация)
7. [AI-агенты](#ai-агенты)

## Код поведения

VPR следует стандартам открытого сообщества. Мы ожидаем:

- Уважительного отношения ко всем участникам
- Конструктивной критики
- Фокуса на том, что лучше для проекта
- Эмпатии к другим участникам

## Как внести вклад

### Типы вкладов

Мы приветствуем различные типы вкладов:

1. **Код**: Исправления багов, новые функции, улучшения производительности
2. **Документация**: Улучшение документации, примеры, туториалы
3. **Тесты**: Новые тесты, улучшение покрытия
4. **Отчеты об ошибках**: Детальные отчеты о проблемах
5. **Предложения**: Идеи для улучшения проекта

### Процесс

1. **Fork** репозиторий
2. **Создайте ветку** для ваших изменений (`git checkout -b feature/amazing-feature`)
3. **Сделайте изменения** следуя стандартам проекта
4. **Добавьте тесты** для новых функций
5. **Убедитесь, что все тесты проходят** (`cargo test --workspace`)
6. **Проверьте код** (`cargo clippy --workspace --lib -- -D warnings`)
7. **Отформатируйте код** (`cargo fmt`)
8. **Закоммитьте** с conventional commits
9. **Push** в вашу ветку (`git push origin feature/amazing-feature`)
10. **Создайте Pull Request**

## Стандарты разработки

### Язык и стиль

- **Язык**: Rust (edition 2021)
- **Минимальная версия**: Rust 1.70+
- **Форматирование**: `cargo fmt` (стандартный rustfmt)
- **Linting**: `cargo clippy --workspace --lib -- -D warnings`

### Стандарты качества кода

Все агенты следуют единым стандартам VPR:

- **Cyclomatic complexity**: ≤ 10
- **Test coverage**: ≥ 85% по изменённому коду
- **Моки/фейки**: Запрещены в продакшн коде
- **Commits**: Conventional Commits формат
- **Документация**: Обновляется вместе с кодом

### Структура кода

```
src/
├── masque-core/     # MASQUE сервер
├── doh-gateway/     # DoH gateway
├── vpr-app/         # Desktop клиент
├── vpr-crypto/      # Криптография
├── vpr-tui/         # TUI
└── vpr-ai/          # AI компоненты
```

### Naming Conventions

- **Модули**: `snake_case`
- **Типы**: `PascalCase`
- **Функции**: `snake_case`
- **Константы**: `SCREAMING_SNAKE_CASE`
- **Тесты**: `test_<functionality>`

### Error Handling

- Используйте `Result<T, E>` для fallible операций
- Избегайте `unwrap()` в продакшн коде
- Используйте `expect()` только с описательными сообщениями
- Предпочитайте `?` оператор для propagation

**Хорошо:**
```rust
fn parse_config(path: &Path) -> Result<Config> {
    let content = fs::read_to_string(path)
        .context("failed to read config file")?;
    // ...
}
```

**Плохо:**
```rust
fn parse_config(path: &Path) -> Config {
    let content = fs::read_to_string(path).unwrap();
    // ...
}
```

### Unsafe Code

- Минимизируйте использование `unsafe`
- Все `unsafe` блоки должны иметь SAFETY комментарий
- Объясняйте инварианты и риски
- Рассмотрите альтернативы перед использованием `unsafe`

**Пример:**
```rust
// SAFETY: libc::geteuid() is a safe system call that returns the
// effective UID of the process. It has no side effects and is
// safe to call from any context.
let euid = unsafe { libc::geteuid() };
```

## Процесс разработки

### Ветвление

- `main`: Стабильная ветка, всегда компилируется
- `feature/*`: Новые функции
- `fix/*`: Исправления багов
- `docs/*`: Изменения документации
- `refactor/*`: Рефакторинг без изменения функциональности

### Conventional Commits

Используйте формат Conventional Commits:

```
<type>(<scope>): <subject>

<body>

<footer>
```

**Типы:**
- `feat`: Новая функция
- `fix`: Исправление бага
- `docs`: Изменения документации
- `style`: Форматирование (не влияет на код)
- `refactor`: Рефакторинг
- `test`: Добавление/изменение тестов
- `chore`: Изменения в build процессе, зависимостях

**Примеры:**
```
feat(masque): add CONNECT-UDP support
fix(crypto): zeroize ML-KEM secrets on drop
docs(architecture): update transport layer description
test(replay): add concurrent stress test
```

### Pull Request Process

1. **Описание**: Детальное описание изменений
2. **Связанные issues**: Упоминание связанных issues
3. **Тесты**: Убедитесь, что все тесты проходят
4. **Документация**: Обновите документацию если нужно
5. **Review**: Дождитесь review перед merge

### Code Review Guidelines

**Для авторов:**
- Отвечайте на комментарии конструктивно
- Вносите изменения по замечаниям
- Объясняйте сложные решения

**Для ревьюеров:**
- Будьте конструктивны и уважительны
- Фокусируйтесь на коде, а не на человеке
- Предлагайте конкретные улучшения

## Тестирование

### Типы тестов

1. **Unit тесты**: Тестирование отдельных функций
2. **Integration тесты**: Тестирование взаимодействия компонентов
3. **Property тесты**: Тестирование инвариантов
4. **E2E тесты**: Полный поток от клиента до сервера

### Запуск тестов

```bash
# Все тесты
cargo test --workspace

# Тесты конкретного компонента
cargo test -p masque-core
cargo test -p vpr-crypto

# С coverage
cargo llvm-cov --html

# E2E тесты
scripts/e2e_full_test.sh
scripts/e2e_automated.sh
```

### Требования к тестам

- **Coverage**: ≥85% по изменённому коду
- **Названия**: Описательные названия (`test_<functionality>`)
- **Изоляция**: Тесты не должны зависеть друг от друга
- **Скорость**: Тесты должны выполняться быстро

### Пример теста

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_replay_protection_blocks_duplicates() {
        let protection = ReplayProtection::new(Duration::from_secs(300));
        let message = b"test message";
        
        // First message should pass
        assert!(protection.check_and_record(message).is_ok());
        
        // Duplicate should be blocked
        assert!(protection.check_and_record(message).is_err());
    }
}
```

## Документация

### Код документация

- Используйте `///` для публичных API
- Используйте `//!` для модульной документации
- Включайте примеры использования где возможно

**Пример:**
```rust
/// Performs hybrid key exchange using ML-KEM768 and X25519.
///
/// # Arguments
///
/// * `server_public` - Server's public key
/// * `client_secret` - Client's secret key
///
/// # Returns
///
/// Shared secret derived from both keys
///
/// # Example
///
/// ```rust
/// let shared = hybrid_key_exchange(&server_pub, &client_sec)?;
/// ```
pub fn hybrid_key_exchange(
    server_public: &PublicKey,
    client_secret: &SecretKey,
) -> Result<SharedSecret> {
    // ...
}
```

### Markdown документация

- Обновляйте документацию вместе с кодом
- Используйте правильный markdown формат
- Проверяйте ссылки на другие документы
- Включайте примеры кода где возможно

### Документация изменений

При добавлении новых функций обновите:

- `README.md`: Если это новая функция пользователя
- `docs/architecture.md`: Если это архитектурное изменение
- `docs/ROADMAP.md`: Если это завершенная задача
- `CHANGELOG.md`: Если есть (создайте если нужно)

## AI-агенты

VPR использует систему AI-агентов для обеспечения качества. См. [`AGENTS.md`](AGENTS.md) для деталей.

### Активация агентов

Агенты активируются через slash-команды:

```
/agents/crypto-sentinel   — для криптографических задач
/agents/dpi-evader        — для задач маскировки трафика
/agents/transport-architect — для работы с MASQUE/QUIC
/agents/security-auditor  — для security review
/agents/e2e-enforcer      — для тестирования
/agents/rust-surgeon      — для Rust кода
```

## Security

### Reporting Security Issues

**НЕ** создавайте публичные issues для security проблем!

Вместо этого:
1. Отправьте email на security@vpr.example (замените на реальный)
2. Опишите проблему детально
3. Дождитесь ответа перед публикацией

### Security Best Practices

- Не коммитьте секреты в репозиторий
- Используйте `age` encryption для секретов
- Проверяйте зависимости на уязвимости (`cargo audit`)
- Следуйте security policies из `docs/security.md`

## Вопросы?

Если у вас есть вопросы:

1. Проверьте документацию в `docs/`
2. Посмотрите существующие issues
3. Создайте новый issue с вопросом
4. Присоединитесь к обсуждениям

## Благодарности

Спасибо всем контрибьюторам! Ваш вклад делает VPR лучше.

---

**VPR** — Stealth VPN для свободного интернета 🌐
