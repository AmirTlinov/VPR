# Infra Scaffold

- `nix/flake.nix` – TBD (сборка Rust/Go/Poetry окружения).
- `terraform/` – описания VPS/portable node (в разработке).
- `ansible/` – плейбуки для `deploy-node`, `sync-content`, `swap-node`.

> На данном этапе создаём структуру каталогов; файлы появятся по мере выполнения TODO.
